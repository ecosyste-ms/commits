<li class="nav-item ms-auto">
  <div class="dropdown">
    <button class="btn btn-light dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
      Sort
    </button>
    <ul class="dropdown-menu">
      <% [['last_synced_at', 'desc', 'Recently Synced'],['total_commits', 'desc', 'Total Commits'],['total_committers','desc','Total Committers'],['mean_commits','desc','Avg Commits per committer'],['dds','desc','Development Distribution Score']].each do |sort, order, name| %>
        <li><a class="dropdown-item <%= 'active' if params[:sort] == sort %>" href="<%= url_for(sort: sort, order: order) %>"><%= name %></a></li>
      <% end %>
    </ul>
  </div>
</li>